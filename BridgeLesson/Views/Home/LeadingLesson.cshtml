
<div class="row" ng-app="BridgeLessonModule">
    <div class="col-sm-12">
        <a href="@Url.Action("Index")" class="btn btn-link">Back to the main page</a>
        <div class="subpage-header">
            <h2>Leading lesson</h2>
            <p class="lead">This site would help you to learn how to lead UDCA in the easy and friendly way</p>
        </div>
        <form name="leadExamplesForm" ng-submit="submit()" ng-controller="LeadController">
            <h3>Are you sure that you know how to lead from all combinations below? </h3>
            <p>Please indicate the correct card in each one and click check. We assume that we lead in the same way on color and NT games.</p>

            <ul class="col-md-12" ng-repeat="dist in LeadDistribution | startFrom:currentPage*pageSize | limitTo:pageSize">
                <li class="list-group-item">
                    <div ng-class="dist.correct != undefined ? (dist.correct ? 'alert-success' : 'alert-danger') : ''">
                        {{dist.key | Tto10}} :
                        <span ng-repeat="card in dist.key.split('')">
                            <input type="radio" ng-model="dist.chosenValue" ng-value="card">{{card | Tto10}}
                        </span>
                    </div>
                </li>
            </ul>
            <p ng-bind="validationMessage"></p>
            <button type="button" class="btn btn-primary" ng-click="proceedBack()" ng-disabled="!canProceedBack">Previous set</button>
            <button type="submit" class="btn btn-primary">Check it</button>
            <button type="button" class="btn btn-success" ng-click="proceedToTheNext()" ng-disabled="!canProceedFurther">Next set</button>
        </form>

    </div>
</div>